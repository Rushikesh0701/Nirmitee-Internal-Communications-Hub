name: Keep Render Alive
on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://nirmitee-internal-communications-hub.onrender.com/api/health
